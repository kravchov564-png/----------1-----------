''''Совет
Для частых операций добавления/удаления используйте list.
Для данных, которые не должны меняться, — tuple.
Для быстрого поиска по ключу — dict.
Для уникальных значений — set.
==========================================================================
# Создание СПИСКИ

numbers = [1, 2, 3]
empty_list = list() # Читаемее, чем []

# Добавление элементов

numbers.append(4)  # В конец → [1, 2, 3, 4]
numbers.insert(1, 1.5)# По индексу → [1, 1.5, 2, 3, 4]

# Удаление

numbers.remove(1.5) # По значению
popped = numbers.pop() # Удаляет и возвращает последний элемент (4)

# Итерация

for num in numbers:
    print(num)
================================================================
КОРТЕЖИ

# Создание

point = (10, 20)
colors = tuple(["red", "green", "blue"])  # Явное преобразование

# Распаковка

x, y = point  # x = 10, y = 20

# Использование как ключа словаря (т. к. неизменяем)

locations = {(35.7, 139.7): "Токио"}
=====================================================================
СТРОКИ

# Форматирование

name = "Alice"
greeting = f"Привет, {name}!" # f-строки (Python 3.6+)

# Методы

text = " Python "
cleaned = text.strip().upper() # "PYTHON"

# Разделение и соединение

words = "a,b,c".split(",") # ["a", "b", "c"]
new_text = "-".join(words)  # "a-b-c"
====================================================================
ДИАПАЗОНЫ

# Создание

numbers = range(5)    # 0, 1, 2, 3, 4
even_numbers = range(0, 10, 2) # 0, 2, 4, 6, 8

# Преобразование в список

list(numbers) # [0, 1, 2, 3, 4]

# Использование в циклах

for i in range(3):
    print(i) # 0, 1, 2
======================================================================
СЛОВАРИ

# Создание

user = {"name": "Alice", "age": 30}
empty_dict = dict() # Читаемее, чем {}

# Доступ

age = user.get("age", 0) # 0 — значение по умолчанию, если ключа нет

# Обновление

user.update({"age": 31, "city": "Berlin"})

# Итерация по парам ключ/значение

for key, value in user.items():
    print(f"{key}: {value}")

# Словарные включения (dict comprehension)

squares = {x: x**2 for x in range(5)} # {0: 0, 1: 1, 2: 4, ...}
=======================================================================
МНОЖЕСТВА
# Создание

unique_numbers = {1, 2, 2, 3} # {1, 2, 3}
empty_set = set() # Не используйте {} — это словарь!

# Операции

a = {1, 2, 3}
b = {3, 4, 5}

print(a | b) # Объединение: {1, 2, 3, 4, 5}
print(a & b) # Пересечение: {3}

# Проверка на вхождение

if 1 in a:
    #print("1 есть во множестве")
=========================================================================

